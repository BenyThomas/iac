# UAT environment

Gate-checked environment for release verification. Only promote changes that have been validated in `dev`.

- Requires Platform + service owner approval for manifest updates.
- Use Argo CD manual sync windows if coordinated testing is needed.
